/**
 * An element with an associated value representing the progress or fractional state of it,
 * for example a progress value with a percent complete
 */
interface FractionalElement {

    void setValue(float value);
    float getValue();
}
